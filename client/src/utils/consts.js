const HOME_ROUTE = '/';

const LOGIN_ROUTE = '/login';

const REGISTER_ROUTE = '/register';

const TODO_ROUTE = '/todoPage';

const ERROR_ROUTE = '/*';

export {
  HOME_ROUTE,
  LOGIN_ROUTE,
  REGISTER_ROUTE,
  TODO_ROUTE,
  ERROR_ROUTE,
};
